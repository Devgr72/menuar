{
  "buildCommand": "npx turbo run build --filter=@menuar/web && cp -r apps/web/dist ./dist",
  "outputDirectory": "dist",
  "installCommand": "npm install",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
