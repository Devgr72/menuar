{
  "buildCommand": "npx turbo run build --filter=@menuar/web",
  "outputDirectory": "apps/web/dist",
  "installCommand": "npm install --ignore-scripts",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
